# Test Harness Image Configuration

# Override network configuration to avoid clash with local-development-dependencies
# when switching between the two
NETWORK_CDP_UPLOADER_SUBNET=10.0.3.0/24
NETWORK_CDP_UPLOADER_IP=10.0.3.20
NETWORK_CDP_UPLOADER_GATEWAY=10.0.3.1
NETWORK_CDP_UPLOADER_MANAGER_IP=10.0.3.2